{
  "kind": "collectionType",
  "collectionName": "columns",
  "info": {
    "name": "columns"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "label": {
      "type": "string",
      "required": true
    },
    "tasks": {
      "via": "column",
      "collection": "tasks"
    }
  }
}
